//TO CHECK THE NUMBER IS NATURAL OR NOT
#include <stdio.h>
int main()
{
    int n;
    printf("Enter the no: ");
    scanf("%d", &n);
    if (n>=1)
    {
        printf("It is natural number");
    }
    else
    {
        printf("It is not a natural numbrt");
    }
    return 0;
}